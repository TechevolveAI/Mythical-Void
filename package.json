{
  "name": "mythical-creature-game",
  "version": "1.0.0",
  "description": "A 2D mythical creature game built with Phaser.js",
  "main": "src/main.js",
  "scripts": {
    "start": "http-server -p 8080 -o",
    "test": "echo \"Opening test framework...\" && http-server -p 8081 -o test-framework.html",
    "validate-flow": "node scripts/validate-game-flow.js",
    "pre-commit": "npm run validate-flow",
    "health-check": "curl -f http://localhost:8080/health || echo 'Server not running'",
    "lint": "echo 'Linting not configured - add ESLint for production'",
    "build": "echo 'No build step required - static HTML/JS game'"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "keywords": ["game", "phaser", "javascript", "mythical", "creature"],
  "author": "",
  "license": "MIT"
}